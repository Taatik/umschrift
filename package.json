{
  "name": "umschrift",
  "version": "1.0.0",
  "description": "a package for transliterating Hebrew to German",
  "main": "index.js",
  "scripts": {
    "build": "rm -rf ./dist && npx tsc",
    "test": "clear && jest",
    "test:dev": "npm run build && node ./dist/index.js"
  },
  "author": "charlesLoder",
  "license": "MIT",
  "dependencies": {
    "havarot": "file:havarot-0.0.3.tgz"
  },
  "devDependencies": {
    "@types/jest": "^26.0.13",
    "jest": "^26.4.2",
    "ts-jest": "^26.3.0",
    "typescript": "^4.0.2"
  }
}
